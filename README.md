# Authentication Service Definition

Available at [buf.build/corux/gps-tracker-auth](https://buf.build/corux/gps-tracker-auth)

Proto definitions are automatically built by a github action and pushed to buf.build.

The generated Go code can be installed via:

```bash
go get go.buf.build/grpc/go/corux/gps-tracker-auth/v1
```
